# Backing Up a Base

Use the backup wizard to select a connection, select a base, choose scope and redaction options, review the plan, run backup, and review the report.

## Related documentation

See the repository `docs/` directory for the source documentation that backs this wiki page.
