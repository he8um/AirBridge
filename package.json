{
  "name": "airbridge",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/desktop run build",
    "test": "npm --prefix apps/desktop run test",
    "check": "npm --prefix apps/desktop run check",
    "release:check": "npm --prefix apps/desktop run check"
  }
}
